edition = "2023";

package a;

option features.json_format = LEGACY_BEST_EFFORT;

enum D1 {
  D1_ZERO = 0;
}

enum D2 {
  D2_ZERO = 0;
}

enum D3 {
  D3_ZERO = 0;
}

enum D4 {
  option features.json_format = ALLOW;
  D4_ZERO = 0;
}

enum D5 {
  option features.json_format = ALLOW;
  D5_ZERO = 0;
}

enum D6 {
  option features.json_format = ALLOW;
  D6_ZERO = 0;
}
